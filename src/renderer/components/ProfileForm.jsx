import React from 'react';
import './ProfileForm.css';

function ProfileForm({ profile, onSave, onCancel }) {
  return (
    <div className="npf-container">
      {/* TODO: Profile form UI */}
    </div>
  );
}

export default ProfileForm;
